# 1.0 release gate

ResearchForge 1.0 is deferred until all MVP success criteria pass, including CLI workflows, the implemented Go + HTMX web GUI workflows, reproducible reports, provenance-backed project restoration, and the remaining `project_license` owner decision.

The license release blocker is `TODO.md:34`. Do not publish a public 1.0 artifact until `make license-decision-live-audit` shows issue #1 has non-placeholder owner approval fields and `make license-decision-approval-gate` passes with `approved:true`:

- License SPDX identifier;
- Copyright holder;
- Approved by;
- Approval date.
