# Web GUI testing pattern

- CLI and web GUI call the same application layer.
- View models are framework-neutral.
- Browser components bind to view models and project artifact APIs/files.
- Tests cover loading, empty, error, and populated states.
- Visualization tests include accessible table/text alternatives where practical.
